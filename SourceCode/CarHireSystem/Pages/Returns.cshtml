@page
@model CarHireSystem.Pages.ReturnsModel

<div class="section-hero">
    <div class="container text-center">
        <h1 class="fw-bold mb-2" style="font-size:2.2rem">Return a Car</h1>
        <p class="mb-0">Enter your booking ID to complete your return</p>
    </div>
</div>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">

            @if (Model.SuccessMessage != null)
            {
                <div class="alert alert-success fw-semibold">&#10003; @Model.SuccessMessage</div>
            }

            @if (Model.ErrorMessage != null)
            {
                <div class="alert alert-danger">@Model.ErrorMessage</div>
            }

            <div class="card p-4">
                <h5 class="fw-semibold mb-1">Vehicle Return</h5>
                <p class="text-muted mb-4" style="font-size:0.9rem">Please have your booking ID ready. You can find it on your booking confirmation.</p>
                <form method="post">
                    <div class="mb-4">
                        <label class="form-label">Booking ID</label>
                        <input type="number" name="BookingId" class="form-control form-control-lg" placeholder="e.g. 4821" />
                    </div>
                    <button type="submit" class="btn btn-navy w-100 py-2">Complete Return</button>
                </form>
            </div>

            <div class="card p-4 mt-4" style="background:#f0f4ff; border-left: 4px solid var(--eh-navy);">
                <h6 class="fw-semibold mb-2">Need help?</h6>
                <p class="mb-0 text-muted" style="font-size:0.85rem">Call us on <strong>0123456789</strong> or email <strong>easyhire@fake.com</strong></p>
            </div>
        </div>
    </div>
</div>
